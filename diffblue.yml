buildCmd: mvn -e -X -Dmaven.test.skip=true compile
testCmd: true
lineLength: 4096
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
#Target TGF: 212.227922078
# - methods: 26
# - complexity: 1461
# - construction: 21.0
# - dependency: 5.78300865801
  - target:
    - dir: RCT-Base/target/classes
      file: org/nesc/ecbd/utils/RedisUtil.class
      sourceDir: RCT-Base/src/main/java
      sourceFile: org/nesc/ecbd/utils/RedisUtil.java
#Target TGF: 48.0227272727
# - methods: 6
# - complexity: 340
# - construction: 5.0
# - dependency: 1.21590909091
  - target:
    - dir: RCT-Dashboard/target/classes
      file: org/nesc/ecbd/service/ScheduleTaskService.class
      sourceDir: RCT-Dashboard/src/main/java
      sourceFile: org/nesc/ecbd/service/ScheduleTaskService.java
#Target TGF: 247.647186147
# - methods: 30
# - complexity: 1271
# - construction: 16.4575757576
# - dependency: 12.7254329004
  - target:
    - dir: RCT-Dashboard/target/classes
      file: org/nesc/ecbd/service/RDBAnalyzeResultService.class
      sourceDir: RCT-Dashboard/src/main/java
      sourceFile: org/nesc/ecbd/service/RDBAnalyzeResultService.java
#Target TGF: 273.5
# - methods: 9
# - complexity: 284
# - construction: 8.0
# - dependency: 7.0
  - target:
    - dir: RCT-Base/target/classes
      file: org/nesc/ecbd/utils/Jemalloc.class
      sourceDir: RCT-Base/src/main/java
      sourceFile: org/nesc/ecbd/utils/Jemalloc.java
#Target TGF: 49.0
# - methods: 9
# - complexity: 98
# - construction: 6.0
# - dependency: 3.0
  - target:
    - dir: RCT-Dashboard/target/classes
      file: org/nesc/ecbd/util/ListSortUtil.class
      sourceDir: RCT-Dashboard/src/main/java
      sourceFile: org/nesc/ecbd/util/ListSortUtil.java
#Target TGF: 316.12987013
# - methods: 38
# - complexity: 1720
# - construction: 25.1666666667
# - dependency: 6.48142135642
  - target:
    - dir: RCT-Base/target/classes
      file: org/nesc/ecbd/utils/ElasticSearchUtil.class
      sourceDir: RCT-Base/src/main/java
      sourceFile: org/nesc/ecbd/utils/ElasticSearchUtil.java
#Target TGF: 92.15
# - methods: 14
# - complexity: 493
# - construction: 8.33333333333
# - dependency: 6.31666666667
  - target:
    - dir: RCT-Dashboard/target/classes
      file: org/nesc/ecbd/service/SlowlogService.class
      sourceDir: RCT-Dashboard/src/main/java
      sourceFile: org/nesc/ecbd/service/SlowlogService.java
#Target TGF: 125.526984127
# - methods: 8
# - complexity: 421
# - construction: 7.0
# - dependency: 2.2873015873
  - target:
    - dir: RCT-Dashboard/target/classes
      file: org/nesc/ecbd/util/SqliteUtil.class
      sourceDir: RCT-Dashboard/src/main/java
      sourceFile: org/nesc/ecbd/util/SqliteUtil.java
#Target TGF: 300.083333333
# - methods: 30
# - complexity: 944
# - construction: 20.3333333333
# - dependency: 16.4833333333
  - target:
    - dir: RCT-Base/target/classes
      file: org/nesc/ecbd/utils/RedisMemCalculate.class
      sourceDir: RCT-Base/src/main/java
      sourceFile: org/nesc/ecbd/utils/RedisMemCalculate.java
#Target TGF: 60.8888888889
# - methods: 9
# - complexity: 355
# - construction: 6.0
# - dependency: 4.5
  - target:
    - dir: RCT-Dashboard/target/classes
      file: org/nesc/ecbd/service/RedisInfoService.class
      sourceDir: RCT-Dashboard/src/main/java
      sourceFile: org/nesc/ecbd/service/RedisInfoService.java
dependenciesOnClasspath:
-
  source: classes
  classFile: org/nesc/ecbd/service/RedisInfoService.class
-
  source: classes
  classFile: org/nesc/ecbd/mapper/RedisInfoMapper.class
-
  source: classes
  classFile: org/nesc/ecbd/utils/RedisUtil.class
-
  source: classes
  classFile: org/nesc/ecbd/utils/RedisMemCalculate.class
-
  source: classes
  classFile: org/nesc/ecbd/service/ScheduleTaskService.class
-
  source: classes
  classFile: org/nesc/ecbd/mapper/RDBAnalyzeResultMapper.class
-
  source: classes
  classFile: org/nesc/ecbd/utils/ElasticSearchUtil.class
-
  source: classes
  classFile: org/nesc/ecbd/entity/RDBAnalyze.class
-
  source: classes
  classFile: org/nesc/ecbd/entity/ReportData.class
-
  source: classes
  classFile: org/nesc/ecbd/util/ListSortUtil.class
-
  source: classes
  classFile: org/nesc/ecbd/entity/RedisInfo.class
-
  source: classes
  classFile: org/nesc/ecbd/service/SlowlogService.class
-
  source: classes
  classFile: org/nesc/ecbd/service/RDBAnalyzeResultService.class
-
  source: classes
  classFile: org/nesc/ecbd/mapper/SlowlogMapper.class
-
  source: classes
  classFile: org/nesc/ecbd/utils/Jemalloc.class
-
  source: classes
  classFile: org/nesc/ecbd/entity/SlowlogEntity.class
-
  source: classes
  classFile: org/nesc/ecbd/utils/RedisUtil$RedisNode.class
-
  source: classes
  classFile: org/nesc/ecbd/util/SqliteUtil.class
-
  source: classes
  classFile: org/nesc/ecbd/report/converseFactory/ReportDataConverseFacotry.class
-
  source: classes
  classFile: org/nesc/ecbd/report/IAnalyzeDataConverse.class
-
  source: classes
  classFile: org/nesc/ecbd/entity/RDBAnalyzeResult.class